#! /bin/csh
#  admin dirname -kill str1 str2 str3....
#  admin dirname -print str1 str2
#  
set dir = $1
shift
if ($1 =~ "-kill" ) then
	shift
	foreach string ($* )
		foreach file ($dir/*)
		   if (-f $file) then	
		         if ($file =~ *${string}* ) /bin/rm $file
		   endif
		end
	end
	
endif

if ($1 == "-print" ) then
	foreach file ($dir/*)
		if (-f $file) then
			if (-r $file) then
			grep $2 $file | grep $3 
		   endif
		endif
	end
endif

